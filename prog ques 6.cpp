class Move
{
private:
double x;
double y;
public:
Move(double a = 0, double b = 0)
{ x=a;
  y=b;
  }// sets x, y to a,

void showmove()
{ 
  cout<<X;
  cout<<y;
}
Move add(const Move & m) 
{
   double newx= m.x+this->x;
   double newy= m.y+this->y;
  move m1 (double newx, double newy)
  { 
    return m;
  }
}
void reset(double a = 0, double b = 0)
{
  x=a;
  y=b;
}
};
